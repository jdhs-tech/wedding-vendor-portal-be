# Docker Environment Configuration Example
# Copy this file to .env.docker and update with your actual values

PORT=5001
SECRET_KEY=your-super-secret-jwt-key-change-this-in-production

# MongoDB Cloud Atlas Connection String
# To get your connection string:
# 1. Go to MongoDB Atlas (https://cloud.mongodb.com)
# 2. Select your cluster
# 3. Click "Connect" -> "Connect your application"
# 4. Choose "Node.js" driver and copy the connection string
# 5. Replace <username>, <password>, and <cluster-url> with your actual values
# Example: mongodb+srv://myuser:mypassword@cluster0.abc123.mongodb.net/wedding_portal?retryWrites=true&w=majority
MONGO_URL=mongodb+srv://<username>:<password>@<cluster-url>/wedding_portal?retryWrites=true&w=majority

NODE_ENV=production

# MongoDB Atlas Setup Instructions:
# 1. Create a free account at https://cloud.mongodb.com
# 2. Create a new cluster (free tier available)
# 3. Create a database user with read/write permissions
# 4. Add your IP address to the IP whitelist (or 0.0.0.0/0 for all IPs)
# 5. Get the connection string and update MONGO_URL above
